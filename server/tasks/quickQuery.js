const { getMovieData } = require('./utils')

const NUM_MEDIA = 10

// think about including backdrop on moviepage

const main = async () => {
  const t = null
  console.log([].concat('a').concat(...t))
}

main().catch((e) => {
  console.error(e)
})
